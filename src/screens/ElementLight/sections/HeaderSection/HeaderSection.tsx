import React from "react";
import { Button } from "../../../../components/ui/button";

export const HeaderSection = (): JSX.Element => {
  // Only render the navigation/header content. Remove the hero/landing section and all decorative/hero image elements.
  return <></>;
};
